@using EntityLayer.Concrete
@model List<WriterMessage>
@{
    ViewData["Title"] = "SenderMessage";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Giden Kutusu</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Konu</th>
                            <th>alıcı</th>
                            <th>tarih</th>
                            <th>Detaylar</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            @* if (item.WriterMessageID == false)
                            {
                                color = "badge badge-danger";
                                mesage = "Okundu";
                            }
                            else
                            {
                                color = "badge badge-success";
                                mesage = "Yeni Duyuru";
                            } *@
                            <tr>
                                <td>@item.WriterMessageID</td>
                                <td>@item.Subject</td>
                                <td>@item.ReciverName</td>
                                <td>@(((DateTime)@item.Date).ToString("dd-MMM-yyyy"))</td>
                                <td><a href="/Writer/Message/MessageDetail/@item.WriterMessageID" class=" btn btn-primary">Mesajı gör</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>



